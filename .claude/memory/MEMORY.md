# Memory Index

- [Hyperion 项目总览](agent-project-overview.md) — **v2(2026-07-28 重规划)**:调度型 agent(编排+记忆+委托);三支柱 P1 调研/P2 bug-RCA★MVP/P3 记忆★特色;三锁定决策 + R0–R5 路线(⚠️ R4/R5 已 2026-08-07 取消,见 [路线复核](harness-route-review-2026-08-07.md));deer-flow/oh-my-pi/code-review-graph 只读参考。
- [踩坑记录文档](pitfall-log.md) — `docs/踩坑记录.md` 位置:项目走过的弯路汇总(每条 现象→弯路→根因→教训→现状);设计前先查、踩坑后往上加。#1 patch rerank 三段反转;#2 漏斗与 opencode 重复→MCP 工具驱动;#3 delegate readline 64KB;#4 MCP cwd 污染;#5 LLM schema 不守;**#6 chunker #58 只修符号路径漏 module 路径(driver_nl80211.c 红鲱鱼、qca-vendor.h 真肇事)**;**#7 ReAct 子 agent 撞 recursion_limit 全丢→优雅降级(astream+裸模型强制收尾;superstep≠轮)**;**#8 @tool 线程池懒导入重模块死锁(import 提顶层;e2e 绿单测红)**;**#9 TurnBudget 中间件 hook 是独立图节点→recursion_limit 每轮=N+2 superstep 非 2(固定 (max+2)*2 撞墙);keying 别用 id(runtime) 每 superstep 变→用 thread_id;单测绿得虚假因手工桩没复刻对象生命周期(写集成探针隔离)**;**#10 opencode 1.18.11 http(streamable-http)MCP 不注册原生工具(agent 绕 curl)→ 用 local stdio(timeout≥120000ms 防首次冷启)**;**#11 glm-5.2 系统性把根因误诊成显眼日志行(连续4次[e2e#2/#4/#5]把 wpa p2p-scan 孤儿误诊成 abort-fail,金标是更早7秒的 scan_res_handler 误路由;apply过≠根因对;e2e#5 验证 soft 时序证伪无效——agent照做但问错问题+记忆先验警告被当假设证伪反噬;**2026-08-10 filter_logs 工具撤销**(deer-flow/omp 双证:opencode grep/awk 等价,踩坑#2;领域知识转 SKILL/prompt;锚定根治靠重心代码+多轮证伪+R5 运行时验证)**;**#12 bug-RCA 流程假设错(一次走完+apply够→工具箱+人在环迭代;validate只验apply,真机oracle;memorize/report验证后才做)**;**#13 skill/prompt 受众错位(面向小白/项目叙事→面向模型指令性;项目知识留踩坑/memory,不进skill;区别于代码注释)**;**#14 build_check 系统软件构建信号歧义(wpa builds=no 是预存环境[无 git tag/缺依赖]非补丁→退出 patch-review 流程,只到 apply;**2026-08-10 工具撤销**,同 filter_logs 逻辑(不编译方针+opencode 能 make))**;**#15 validate_patch 对 agent 传参 marshalling 脆弱(agent rstrip 末尾换行→git apply "补丁损坏"误判→工具入口 normalize LF+末尾换行)**;**#16 只读鉴定 skill 要 bash:deny(edit:deny 挡不住 bash git apply 改用户仓;e2e agent 偷贴补丁→patch-review bash:deny+edit:deny)**。
- [设计前先调研+参考 deer-flow](research-deerflow-first.md) — 每个模块设计前必须先深入调研前沿并精读 deer-flow 对应实现。
- [设计前必查过度设计+最新最佳实践](avoid-overengineering.md) — 牢记:设计前充分评估是否过度设计(YAGNI)+ 查 2025-2026 最新最佳实践;用户对复杂度的直觉通常对(rerank 三段反转教训)。
- [委托型 agent 不建平行管线](delegate-already-localizes.md) — 建 Hyperion 能力前先问"opencode 会不会?";会→做 MCP 工具给它调,别重造(opencode 本就强定位;漏斗→工具反转 = 踩坑 #2)。
- [代码我自己写+大白话讲解](workflow-show-code-in-window.md) — **2026-08-11 反转**:代码(含 .py/config/test/记忆/文档)我直接 Write/Edit,**不再窗口展示让用户手敲**;同时用大白话+比喻讲清在干啥(面向小白);验证我执行并打印测了啥+实际;commit 显式路径、push 单独确认。
- [对齐 deer-flow,目标生产级非 demo](align-to-deerflow-production-grade.md) — 实现优先照齐 deer-flow;最小实现可起步但必须迭代到生产级。
- [生产级补齐待办清单](backlog-production-grade.md) — "最小实现→生产级"跨阶段待办 #1–#44;**v2 新借鉴 #38–#44**(Aider repomap/Agentless 漏斗/mini-swe-agent ACI/OpenHands 记忆/graphiti 时序/委托多档/C-RCA 论文)。
- [注释面向小白](comment-style-beginner-friendly.md) — 注释/docstring 用大白话+类比讲清每个库的作用,别晦涩、别默认读者懂。
- [Python语法.md 不提交](commit-python-syntax-notes.md) — 2026-07-31 再次反转:以后**不提交** Python语法.md(个人笔记);commit 用显式路径,**别 `git add -A`**;规则历史反复以最新为准。
- [oh-my-pi 调研与后续设计演进](oh-my-pi-research-and-design-evolution.md) — 2026-07-27 报告 + **v2 更正**:三层栈是用户心智模型(omp 三件未串成管线);记忆改自建 MemoryService(借 mnemopi 巩固),omp 同时是委托目标。
- [DeepSeek 结构化产出踩坑](deepseek-structured-output-gotcha.md) — DeepSeek-v4-pro 思考模式不支持 tool_choice/response_format json_schema;结构化产出改"喂 Schema+直出 JSON+解析"(R2 委托契约同样适用)。embedding/rerank 走 DashScope 不是 DeepSeek。
- [测试步骤+结果在窗口打印](show-test-steps-and-results-in-window.md) — 跑测试/验证时正文里写清"测了啥+期望+实际(绿/红)",不只甩 Bash 输出块。
- [workspace 设计决策](workspace-design-decision.md) — 2026-07-29 定稿(+07-31 简化):bug-RCA 每 bug 一个 workspace 目录(七段)+本地默认/Docker R5+大日志过滤(filter_logs MCP 工具,addr2line/折叠 defer R5)+补丁 git diff 观察+validate Tier0+复用 deer-flow sandbox;R2末最简/R3完整/R5 Docker。
- [多阶段委托决策](multi-stage-delegate-decision.md) — 2026-07-30 定稿 + **07-31 反转(#54-rework B)**:delegate 拆 localize→repair 两阶段(解 glm-5.2 单loop不收敛);R3.1 弃「多候选采样投票」改**迭代 verify-refine(B)**(同 opencode 会话双循环 + verdict 证伪自审 + validate_patch 执行门控);**07-31 patch 投票 rerank 整体移除**(无 oracle 平凡白烧 token)。
- [rerank 投票适用边界](rerank-mechanism-where-it-shines.md) — **2026-07-31 patch 投票 rerank 整体移除**(无 oracle 平凡白烧 token;现代 SOTA 转单轨迹+执行验证);检索 rerank 保留;有 oracle 再评估,不预建。
- [runtime 中间件策略](runtime-middleware-policy.md) — 2026-07-30:不抄 deer-flow 30+,pull-by-need 加(R3.0=2/R3.2=5-8/R5选配);扩展口已留(middleware 列表+state_schema 自动合并+TypedDict+tool_output 沙箱钩子);将来 skills/MCP/鉴权/沙箱/artifacts/前端 R4/R5 加而不改;@Next/@Prev 链>7 再移植;记忆自建不抄。
- [opencode MCP 接线硬细节](opencode-mcp-wiring.md) — 2026-08-03 源码核实:opencode 配置用顶层 `mcp` 键(非 mcpServers)、`command` 单数组、env 字段叫 `environment` 且 local **不展开 `{env:}`** → codebase 走进程 env 继承(HYPERION_CODEBASE);工具名 `server_tool`(单下划线);坑 #33397/#16491(子 agent)/listTools 5s+#17099/<8KB/PYTHONUNBUFFERED。R3.1 把 Hyperion 能力作 MCP 工具给 opencode 调。**2026-08-06 +踩坑 #10**:opencode 1.18.11 http(streamable-http)MCP **不注册原生工具**(agent 绕 curl)→ **用 local stdio**(e2e 证 7 工具全原生触发;timeout≥120000ms 防首次 recall/search 冷启超时;模板已改 120000 `6338e85`)。
- [R3.2 research e2e 交接](r32-research-e2e-handoff.md) — **2026-08-03 R3.2 e2e GREEN**:`hyperion research` 三闸全过(§5报告/80引用0幻觉/recall命中codebase_fact/8条入记忆);跑通中修 4 真 bug:recursion_limit 单位错(superstep≠轮)、memorize 没 await、子agent不收敛全丢→**优雅降级(astream+强制收尾)**、@tool线程内懒导入死锁→提顶层。**已 commit `c03008c`**(29 文件 +1917 −94);优雅降级=踩坑 #7、线程死锁=踩坑 #8 均已入 `docs/踩坑记录.md`;下一步 R3.3/R3.4。
- [R3.2.x P1 TurnBudget e2e 交接](r32x-p1-turn-budget-handoff.md) — **2026-08-03 e2e GREEN + 已 commit**:TurnBudget warn@N-1+strip@N 治 research 子 agent 撞 recursion_limit(deer-flow turn 轴也是 catch,Hyperion 创新);e2e 暴露并修了 **2 个真 bug(踩坑 #9)**:① keying 用 `id(runtime)` 每轮清零→改 `thread_id`(单测加 keying 回归);② recursion_limit 漏算中间件节点(每轮=N+2 superstep 非 2)→改按 `len(middleware)` 动态算;统一 no-JSON 补救保旧恢复。81 测绿;四闸全过(hard-stop@20/20 无 recursion / 267引用 / 0幻觉 / 10条入记忆 / recall命中)。
- [Skill 子系统设计](skill-design-decision.md) — **2026-08-04 设计完成;⚠️ 2026-08-07 pivot 后 S1–S5 暂缓 YAGNI**(opencode 原生发现 .claude/skills/ 已工作,等跨 agent 再建):opencode 原生吃 SKILL.md(自动发现+`skill()`工具+`permission.skill`),Hyperion 只当仓库+物化器**不自建激活**(踩坑 #2:委托型别重造 delegate 能力);不建 deer-flow 式 SkillActivationMiddleware(Hyperion lead agent 不吃 skill)。S1 仓库+loader/CLI → S2 delegate 物化 → S3 bug_rca `--skill` 透传 → S4 cve-patch 样例;S5 lead 激活 pull-by-need。完整设计 `docs/设计/skill-design.md`。
- [引 arXiv 前先核验](verify-arxiv-cites-before-commit.md) — design doc 引 arXiv 论文前,先 WebFetch `arxiv.org/abs/<id>` 核验标题/数字/章节吻合再 commit;上轮初稿 4 篇里 2 篇描述错(2512.12117 数字夸大、2607.00895 张冠李戴)。
- [R3.4 ingest 交接](r34-ingest-handoff.md) — **2026-08-04 commit `ee8b07a`(已 push origin/main)**:`hyperion memory ingest`(报告 .md/.txt/.pdf + 补丁 .patch/.diff → 记忆);**补丁 id 按 diff 内容算(非 LLM summary,防同 patch 重复入库)**、不新增 kind(bug_lesson+tags)、retrieve 降级;e2e GREEN(report/patch recall 命中 + 同 patch conf 0.30→0.43 合并);e2e 活捉 R1 extract.py kind_detail 整批校验脆弱 → **已修 `b9bfa84`(逐条容错,7 单测)**。
- [R4.1 PR 批量分析设计](pr-review-design-decision.md) — **2026-08-04 设计完成,待施工(post-R3)**:给一组 GitHub PR 链接→抓 diff→分析→去重记忆→跨 PR 聚合(模块/安全/功能)→报告;**复用 ingest/deep_research/CRG,新东西仅三块**(GitHub 抓取/批量编排/聚合报告);**用户拍板:分层深度 + R4.1 定位**;真 gap=补 `symptom` 修跨 PR `_same_subject` 去重;扩 structural.py:59 接 CRG 影响面(填 :44 backlog);安全分层(risk_score+keyword 预筛→子集 LLM);不加 kind/scope(用 tags+source+commit_sha);SmartNote 核验过、ToM 张冠李戴已拦。完整 `docs/设计/pr-review-design.md`。
- [R3.5 报告精修交接](r35-report-handoff.md) — **2026-08-04 commit `350e05a`(未 push)**:bug-RCA 报告 88 行 5 段 → **8 段去重**(问题描述/根因分析/定位定界/关键证据/补丁说明/验证与过程/下一步建议/附录;snippet 只在§四一次防 AI 味);`render_report(state)` 单源;schema 加 optional 字段(problem_summary/impact/scope_notes/log_evidence/patch_rationale/next_steps)+ evidence items 收紧;METR 前置 TL;DR+§六;e2e GREEN(glm-5.2 真 delegate,根因定位准确)。**两 delegate 观察**:evidence 曾只给文件名(已修);verified=False 尽管 apply/revert 都过(opencode JSON 抽取失败丢 verdict,踩坑#5 延伸,未修)。
- [类似-bug 记忆检索 roadmap](similar-bug-recall-roadmap.md) — **2026-08-06 调研结论**:定位**类似**问题(非同一问题)的记忆检索**该做**(2025-2026 学界工业主流:Repository Memory/RepairAgent/𝑀𝑒𝑚ᵖ,非 YAGNI;区别于 exact-duplicate short-circuit 才是 YAGNI)。Hyperion **检索栈是参考仓最强**(4 路 RRF+rerank+衰减)但**喂法最弱**(delegate 自觉调,3 gap:缺确定性预注入 / BugLesson 没填 symptom·fix_patch·blast_radius·commit_sha / 无自动 query)。e2e 实证机制成立+安全(verify-refine 拦盲信)。**P0 定 R3 收尾**(填字段+确定性预注入;不放 R4.1,因是 bug_rca 自己的改动,R4.1 白捡);symptom gap 与 R4.1 同源、按生产者各归各位。
- [记忆只追加指令(已落地)](memory-append-only-directive.md) — **2026-08-06 用户指令 + ✅已落地**:Hyperion 记忆改**只追加写入 + 检索时最新为主、旧作参考**(对标 mem0 v3,arXiv:2504.19413;写入时消歧→检索时推理)。已改:删 memorize `supersede` 只追加(保留 merge 同事实增强)+ 松 recall 过滤(旧版本不再隐藏,作参考;手动 invalidate 错卡仍隐藏)+ RecallHit 加 `created_at`/`superseded_by`(render 显日期);"最新为主"复用 decay,不预建 subject dedup。未走纯检索时极端(失真风险),保留 bi-temporal 骨干仅排名。R4.1 文档 6 处 supersede→检索时同步。34 memory + 15 bug_rca 测绿。
- [opencode serve persistent 调研(③#55)](opencode-serve-persistent-research.md) — ⚠️ **2026-08-07 pivot 后 obsolete**(delegate cold-boot 前提消失 + D0 http + lazy 已覆盖,详见记忆末段);原 2026-08-06 查实如下:`-s/--session <id>` 精确续早有(当前单 bug 单 cwd 流程近乎零增量);`opencode serve`+`run --attach`/REST(`POST /session/{id}/message`、SSE `/event`)是官方免 MCP cold boot 路径,Critique 生产在用(含 session 失效→降级新 session 回退)。**真痛点=MCP cold boot**(hyperion mcp serve 加载 sentence-transformers ~1.2GB,K1+K2≈4×/bug),③ 是纯性能优化。实施=新 `OpenCodeServeDelegate` 后端(serve 生命周期+httpx+session 记账+失效降级);Python 用 httpx 直连 REST 不引社区 SDK。本轮已做 P0 记忆闭环(②[a]+②[b]),③ 拆出单独一轮。
- [R3 记忆闭环 P0 交接](r3-memory-closure-handoff.md) — **2026-08-06,commit d70b40d(未 push)**:②[a] node_report_memorize 填齐 BugLesson 4 字段 + ②[b] 确定性 `recall_lessons` 预注入节点(svc.search memory-only,hybrid;6 节点图)+ 报告/补丁归档到 workspace。e2e GREEN(verified=True/根因准/patch apply);②[a] before/after 对照;②[b] 召回链+trigger→prepend 全验证。**⚠️实测:delegate 产次优补丁**——verified=True 但走 35s 超时兜底(非金标的误路由落点立即释放),verify 门控只查 apply 不查语义最优;逼近金标需 R5 Tier1 运行时验证。观察:纯日志驱动(无--trigger)→trigger=""→跳过预注入,P1 自动query补。③ 拆出单独一轮。
- [Harness 转向交接](harness-pivot-handoff.md) — **2026-08-06 用户拍板 + commit `47654bd`(已 push origin/main)**:Hyperion 从「调度型 orchestrator」转向「**tool+skill server / 领域 harness**」(bug-RCA 主路径 = opencode + bug-rca skill + 8 hyperion MCP 工具,agent 自驱、能自纠;orchestrator 降级留参考)。已落:D0 MCP Streamable HTTP(解 ③ cold-boot)+ 4 新工具(blast_radius/validate_patch/export_patch/**export_report**)+ bug-rca SKILL.md(**9 步 4 硬门**)+ hyperion-bug-rca opencode agent(validate/export_patch/memorize/export_report 硬门)+ CLAUDE.md 身份。e2e 绿:opencode 自驱 recall→search→blast→edit→validate(×2 抓补丁缺陷逼修正)→memorize 入库,不走 orchestrator;还 recall 出旧假设(conf=0.35 覆盖竞态)并用日志证伪 = 先验→证伪闭环生效。✅ **落盘补丁硬门 done(`ab11d56` push + e2e#3 绿:7 工具全原生触发 export_patch 落盘 wpa.patch;踩坑#10 http MCP 不注册→用 stdio)**;✅ **报告落盘硬门 done(`92b5df4`+`3ecfd73` 已 push origin/main;e2e#4 绿:8 工具全原生,export_report 写出 `-rca.md`;① 只落补丁/报告仍只在聊天=被标开放 gap 挂着 → 第 8 工具 export_report,content 参数 → `<repo>-rca.md`,空内容自检;跟 export_patch 对称,排 memorize 后含 id;skill 9 步 4 硬门)**;下轮 P-A patch 分析(build_check + patch-rca skill + GitHub 抓取 + auto-clone + Gerrit stub)。
- [bug-RCA skill 工具箱+人在环](bug-rca-skill-toolbox-hitl.md) — **2026-08-07 二次演化(`deeab6c` 已 push)**:固定流水线(9步4硬门单session)→ **工具箱+人在环迭代**(Anthropic "Build Skills Not Agents" / POPPER ICML2025 迭代证伪 / RepairAgent ICSE2025 补丁-验证循环 / METR 自动验证不够 → 真机是 oracle);memorize/export_report 降为**验证后收尾**(可跨 session);validate 只验 apply;e2e 跑到 export_patch 交人验证。
- [skill/prompt 写作风格](skill-prompt-writing-style.md) — **2026-08-07 用户定(feedudback)**:SKILL.md/agent prompt 受众是**模型**(指令性:祈使/Use when),不是人;**不要**项目内部知识(踩坑编号/模型误诊史/e2e 复盘/对标论文/commit 日期)、不要"面向小白"教学;description=触发器(what+when);教训提炼成可执行指令别叙事;精简。背书:Anthropic Agent Skills 最佳实践。**区别于代码注释**([[comment-style-beginner-friendly]] 面向小白;skill/prompt 面向模型)。
- [P-A 阶段1a 交接](p-a-1a-handoff.md) — **2026-08-07 P-A 补丁/PR 分析线全完成 ✅**(1a+1c+1d+1b)。1a 单补丁/PR 鉴定 tool+skill(patch-review);1c patch_search 工具;1d GerritFetcher(剥 XSSI+base64);**1b 批量聚合报告 batch workflow**(`workflows/patch_report/` 镜像 deep_research + CLI `hyperion patch-report`:fetch→analyze(validate+CRG risk+cited-reporter)→aggregate(分桶+LLM 综合)→report(cited+Verifier)→memorize)。CodeGraph 扩 wrap CRG analyze_changes/community_ids_for。e2e(wpa.patch 真 LLM+真 CRG)GREEN:cited summary 准确命中金标根因。commits fc49c68/41690a7/6e5018c/55144c4。封顶 apply(非 build/test);踩坑 #14/#15/#16。
- [2026-08-07 路线复核](harness-route-review-2026-08-07.md) — **pivot 后全面审视**:R4/R5 取消(本地 harness 非 SaaS)+ obsolete 一批(③/Tier1/build_check/artifacts/lazy-load/Skill S1-5)+ 收敛三件事(代码情报/记忆/skill+工具)+ 不编译强化(编译/测试/复现全用户自验)+ 核心顺序 filter_logs→多库→2a→2b。权威:CLAUDE.md「路线复核」段。
- [2026-08-10 全工具审核后工具集](toolset-after-audit-2026-08-10.md) — **两批撤**:① commit 6cc6ea4 撤 filter_logs/build_check/patch_search;② 撤 demo @tool 链路(sandbox/code_nav/memory @tool + registry + platform/agent + run/tools + config tools 段 + 占位 delegate)。**留 9 MCP 工具**(6 差异化核心 + 3 确定性硬门)+ CLI 基建 + research/patch_report workflow + services 底座。判据:opencode 能做的灵活类撤、硬门/差异化留。
- [多库地基 per-call codebase 交接](multi-codebase-per-call-handoff.md) — **2026-08-11 路线 #1 多库地基收尾完成**:数据层(code_index table-per-repo + 记忆 codebase 列/Scope 隔离)早就绪,gap 全在 MCP 工具层。把 `blast_radius` 的 `codebase or repo` per-call 模板推广到 `memory_recall`/`memory_memorize`/`search_codebase`(各加可选 `codebase`,数据层零改动);FastMCP schema 4 工具全暴露 codebase(required=False);13 测绿+3 新 per-call 测(`_FakeMemSvc` monkeypatch 绕网络)。解锁路线 #2(feature 2a 调用链)。**⚠️gotcha:既有 `test_memory_recall_kind_filter` 挂真 DashScope 网络,无 key 环境用 `-k "not kind_filter"` 跳过。**
- [路线#2 call_chain 交接](route2-call-chain-handoff.md) — **2026-08-11 路线 #2(feature 2a 调用链)完成,第 10 个 MCP 工具**:`CodeGraph.call_chain`(符号种子+仅 CALLS 边+N 跳双向 BFS+PageRank,填 P1.5 caller/callee 「延后」)+ MCP 工具壳(镜像 blast_radius 降级)。与 blast_radius 互补(符号·CALLS-only·调用链 vs 文件·全边·波及面)。**gotcha:`nx.pagerank` 3.6.1 走 scipy 后端,本机无 scipy → 写 `_pagerank` 辅助降级 `_pagerank_python`(纯 python 不 OOM;弃 `_pagerank_numpy` 稠密矩阵)**。CRG 方法实际叫 `_batch_get_nodes`(计划写错为 `get_nodes_by_qualified_names`)。8 测绿+happy-path probe 实证(alpha→callers Foo.method/caller,callees beta/gamma→delta hop2)。**工具 gotcha:后台并行 uv run 争同一 .venv + cwd 漂到 code-review-graph 子项目 → 串行+先 `cd` 项目根**。下一步路线 #3(feature 2b cross_version_diff)。
- [路线#3 cross_version_diff 交接](route3-cross-version-handoff.md) — **2026-08-11 路线 #3(feature 2b 跨版本对比)完成,第 11 个 MCP 工具**:`cross_version_diff`(`code_graph.py` **模块级函数**,非 CodeGraph 方法 —— git 核不需图实例):同仓两 git ref(base..head)→ 提交门(`git log`)+ `git cherry` 等价摘要 + concern 的 `git diff` 文本 + (有图)`touched_functions`。**git 为核图可选** —— 没 CRG/图也能跑(降级强于 blast_radius/call_chain)。复用 CRG `parse_git_diff_ranges`+`map_changes_to_nodes`+`_SAFE_GIT_REF`+2a `_batch_get_nodes`;CRG 缺口(无 build_at_ref/单图单侧/graph_diff 休眠)由 git 核绕开。**对 spec 3 偏离(pivot 后修正)**:不内置 LLM(判断归 agent)/ 单仓两 ref 非 repo_a-repo_b / git 为核图可选。补**版本时间轴**维度(blast_radius=空间·call_chain=调用链·cross_version_diff=版本时间)。24 测绿(ruff 干净;正向含 CRG touched_functions 实跑 + bad_ref/not_a_repo 2 降级 hermetic)。下一步路线 #4(记忆自动 query P1)。
- [路线#4 记忆自动 query P1 交接](route4-auto-query-handoff.md) — **2026-08-11 路线 #4(记忆自动 query P1)完成,代码完未 commit**:补「修复阶段从不召回」真故障。**A1 `_log_digest`(日志确定性摘要当 query)被探针证伪→转 B**:真 demo2 日志 tail 32KB 全音频噪声、全扫 X11 BadWindow 淹没 wpa 信号 → 确定性日志切片分不开相关/无关错误(同 2026-08-10 砍 filter_logs,踩坑#11,别再在 Hyperion 做日志切片)。**B**:定位后加确定性 `recall_for_repair` 节点,拿 LLM 产的 `problem_summary`(零额外 cost)当 query 召回历史修法,预进修复 prompt(带「不是答案,别照抄」安全 nudge,防踩坑#11)。OM-RAG(arXiv 2607.21911v1)背书:无检索诊断 0.238 vs 结构化检索 0.931(+291%),query=bug 描述本身。7 节点链 ingest→recall_lessons→localize_loop→recall_for_repair→assemble_repair→repair_loop→report;**参考实现非主路径**(主路径=opencode+skill+MCP);13 recall 测 + 33 bug_rca 目录测全绿 + graph 7 节点编译 OK + ruff 干净。CLAUDE.md 核心顺序 #1-#4 全落地。
- [#38 repo-map 交接](route5-repomap-handoff.md) — **2026-08-11 backlog #38 完成,第 12 个 MCP 工具 `repo_map`**(代码情报延伸):Aider repomap 式全仓 PageRank 符号地图(delegate 全局视角 / 调研「关键模块」骨架)。**B 路线(用户拍板,偏离 backlog 原文)**:不新建 repomap.py、不抄 tags.scm —— 精读发现 CRG 已抽好 CALLS 边 + Hyperion 已有 `_pagerank`(route2)。改 `CodeGraph.repo_map()`:整图 CALLS 子图 → `_pagerank`(全局非种子邻域)→ 降序贪心填 token 预算 → 按文件分组树(`_render_repomap_tree`)。零新依赖、零 tags.scm 维护(重抄=踩坑#2)。**探针实证(wpa 真图)**:149 符号/69 文件 塞 2048 预算;top=wpa_cli_cmd/send_and_recv_msgs(结构核心,合理)。**修 2 真 bug**:① CRG 存绝对路径 → 渲染剥公共前缀 + 符号去路径前缀(否则全图被 abs path 淹没);② token 估算用「显示名」非全长 qn(否则虚高 2.5x 早停 → packing 67→149 符号);渲染器加空 files 守卫。30 测绿 + ruff 干净。backlog:签名富化(CRG `::` ↔ parser `.` 格式坑)/CLI(对齐纯 MCP)/非 CRG 降级。
- [上游 commit 合入评估交接](upstream-merge-handoff.md) — **2026-08-11 低优 backlog #1 完成,第 13 个 MCP 工具 `merge_eval` + `upstream-merge` skill**:三态(已修/建议合/冲突)逐 commit 评估。`merge_eval`(code_graph.py 模块级函数,cross_version_diff 姐妹,git 为核图可选):patch-id 等价→`already_fixed` + `git apply --check`→`recommend_merge`|`conflict`;skill 负责拉上游本地+checkout fork_ref+查相关性+报告。**⚠️关键 gotcha:patch-id 等价检测用「取反集」非「收集=标记」** —— 3 探针证 git 2.x 的 `--cherry-mark`/`--cherry-pick`/对称差都是把等价 commit **剔除**不标 `=`,故 `shown=--cherry-pick --right-only fork...upstream_head`,`equiv=commit_shas−shown`。抽 `_run_git` 共用(cross_version_diff 回归绿)。hermetic 测三态全对 + **真仓 wpa 探针**:range 3 commit 全正确判 already_fixed(equiv 优先于 applies=False,三态优先级对)。35 测绿+ruff 干净。backlog #60(apply 检查 worktree→merge-tree 不 touch worktree)。**cwd 漂 gotcha:探针/调用用绝对 repo_path**。
- [recall 价值验证交接](recall-validation-handoff.md) — **2026-08-11 backlog #59 验证完成:正向(N=1/臂)**。recall 一个"类似 bug"教训 → 定位**有增益**:用 demo2 同类的真 bug(interworking_select 覆写 scan_res_handler→p2p-scan 泄漏)跑 baseline(空记忆) vs recall(有种 demo2 教训)对比——两臂根因/补丁都 CORRECT,但 recall **1 轮收敛(vs 2)、steps 11 vs 32(3×↓)、tokens 91K vs 390K(4×↓)**,补丁还选了类级中央修复(略胜)。隔离靠 `(owner,codebase)` 硬分区+两份 repo 副本(demo2_base/demo2_rec);检索闸先过(教训召回 score=检索上限 0.0279,校准证非弱匹配)。**解释老矛盾**:同 bug recall 偏负(先验=上次答案冗余/锚定)vs 类似 bug recall 增益(模式迁移)。决策:recall 保留+继续投,B(P1 自动 query)值得做(建议先补 1 轮确认)。不改产品代码;Candidate A 是真 wpa bug(补丁可单独提)。
- [Tier2 #5 index 前置门槛交接](tier2-index-prerequisite-handoff.md) — **2026-08-11 Tier2 #5 完成,代码完未 commit**:① `hyperion index` 改**一键建向量索引+结构图**(CodeGraph.build;已建/非force 跳过;CRG 没装或失败非致命降级;新 `--no-graph`)——降级提示「先建 hyperion index」从误导死路变真;② **bluez 实测活捉并修 blast_radius 路径 bug**:CRG 存 `repo_root` 前缀路径,agent 给仓库相对路径→`get_impact_radius` 精确匹配落空→静默返 0(5744 变 0);`impact_radius` 加 `_resolve_file_paths`(精确>endswith 后缀兜底>原样)。**真机 bluez(18790 chunk/194M 向量 + 133M 图)** 4 工具全通(search/call_chain/repo_map + blast 改后 175 changed/5744 impacted)。code-test/ gitignore(54M 第三方源码)。36 测绿+ruff 全项目干净。Tier2 #4(P-A 遗留)未做。
- [Tier2 #4 P-A 遗留交接](tier2-pa-gerrit-lineverify-handoff.md) — **2026-08-11 Tier2 #4 第 1 档完成,代码完未 commit**(用户拍板「Gerrit 修+验证加强」;--deep/语义去重缓 YAGNI)。三个真 bug:① GerritFetcher 无鉴权(私有/限速 403)→ 加 `username`/`http_password`(默认 env `GERRIT_USERNAME`/`GERRIT_HTTP_PASSWORD`,对齐 GITHUB_TOKEN),有凭据走 `/a/`+`httpx.BasicAuth`;② `from_config()` 永远返 GitHubFetcher→Gerrit URL 静默失败 → 新模块级 `fetcher_for_url(url)` 按 URL 选(GerritFetcher._GERRIT_RE 命中→Gerrit),`node_fetch_prs` 每条 URL 各取;③ 报告只查文件级 → 新 `diff_hunk_lines`(解析 `@@ -a,b +c,d @@` 取新侧行区间)+ `verify_and_append` 加**行锚定软查**(line ∈ hunk=锚到,否则未锚定;只提示不删,合法引上下文也可能未锚定),file 仍是硬指标。**bluez 真补丁探针(e81b6b9 PBAP 堆溢出+gatt-client UAF)三态全对**:pbap.c:331∈[330,337]锚定 / gatt-client.c:9999未锚定 / src/foo.c编造可疑。27 测绿(7 新)+ 231 全绿回归 + ruff 干净。gotcha:旧 verify 测不传 artifacts→无 hunk→行锚定分母 0 不报率(回归旧行为);断言别用「未锚定 not in md」(会误中说明文字「引上下文也可能未锚定」)。
